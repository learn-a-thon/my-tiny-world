server:
  port: 10152

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/searchapi
    driver-class-name: org.mariadb.jdbc.Driver
    username: search
    password: search
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
